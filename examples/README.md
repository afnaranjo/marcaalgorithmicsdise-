# Ejemplos

`approved/` contendrá únicamente piezas representativas aprobadas; `experimental/` contendrá exploraciones claramente etiquetadas. Una referencia visual no se convierte automáticamente en plantilla ni en asset reutilizable.
